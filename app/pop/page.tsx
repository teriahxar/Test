import { UniverseDashboardPage } from "@/components/universe-dashboard-page";

export default async function PopPage() {
  return <UniverseDashboardPage universeSlug="pop" titleOverride="POP! World" descriptionOverride="Fun finds, iconic pops, and collectibles worth keeping an eye on." />;
}
